
# list here commands to run when the user runs './run URL_FILE'
# any lines that start with # or are blank won't run. 
# (and actually any lines that include # in them at all won't run
# so that may need to be adjusted if it ever ends up mattering)

println!("Hello, world!");
ls
python3 Metricizer.py input_file.txt

